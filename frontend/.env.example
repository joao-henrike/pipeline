# ── Configuração do servidor frontend
PORT=3000

# URL do backend FastAPI (IP privado ou público do EC2 backend)
# Injetada pelo Terraform no user_data ao criar as instâncias
BACKEND_URL=http://BACKEND_EC2_IP:8000
